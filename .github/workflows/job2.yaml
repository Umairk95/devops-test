name: Job  Dependency
on:
  push:
    branches:
      - job2

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run Job 1
        run: echo "Running Job 1"
