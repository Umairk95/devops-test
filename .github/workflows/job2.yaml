name: Second Workflow

on:
  workflow_run:
    workflows: ["First Workflow"]  # This references the name of the first workflow
    types:
      - completed  # Runs after the first workflow completes (success or failure)

jobs:
  follow_up:
    runs-on: ubuntu-latest
    steps:
      - name: Run follow-up job
        run: echo "Running this job no matter what!"
